<div class="cardContainer">
    <h1>Hello World</h1>
    <% some.forEach(card => { %>
        <%- include('menucard', {
            title: card.title,
            desc: card.desc,
        }) %>
    <% }); %>
</div>